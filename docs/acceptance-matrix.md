# RSI Atlas Acceptance Matrix

This is the live completion ledger for the normative criteria in Section 33 and the verification evidence in Appendix D of the approved specification. `Not proven` means current evidence is missing, partial, indirect, stale, or narrower than the criterion. A criterion moves to `Proven` only with a current source, automated check, and runtime or release artifact where the requirement demands one.

## Section 33

| Criteria | Area | Current status | Current evidence | Closing evidence |
| --- | --- | --- | --- | --- |
| 1–9 | Product and UX | Not proven | Phase 1 proves a real grouped Command Center, exact remediation/stale states, keyboard refresh, semantic identifiers and VoiceOver order, compact/typical sizing, Light/Dark, increased contrast, large text, Reduce Motion, and multi-window behavior in the unsigned development app. | Presets; complete workspaces/canvas/inspector/report/labs; release-artifact accessibility acceptance. |
| 10–24 | Ingestion | Not proven | Phase 2A through `113110c` directly proves the exact-duplicate half of criterion 10; strict schema validation at native/API/CLI/storage boundaries for criterion 17; hard engine-death recovery without duplicate durable publication for criterion 19; append-only acquisition/decision/outbox evidence and replay-conflict rejection for criterion 23; and development zero-egress/raw admission portions of criterion 24. Phase 2B through `6383861` adds development-only governed Tier-0 parse (qualified `pypdf`), Seatbelt worker isolation (including Keychain Mach denial), preflight-before-parse composition with Process PDF admission/assessment gating, append-only parser-attempt journals, CAS-first canonical JSON versions, and native/API canonical-page inspection—without production promotion, Docling/Tier-1, OCR/scanned fallback. Phase 2C through `a51545d` adds five implemented chunking families, frozen intrinsic chunk goldens (criterion 14/22 development portions), CAS-first chunk-set persistence, and loopback chunk inspect APIs—without claiming criterion 15 production-ready parent-child/table policy. Phase 2D through `63b0653` adds development fixture dense (pgvector) + lexical (PostgreSQL FTS) staging indexes, exact-identifier hits, and atomic activate/rollback publication (criterion 16/18 development portions) with searchable=false until active pointer flip—without production embedding-model promotion, Tantivy, or hybrid retrieval. Independent 2B re-review: approve-with-nits (Important blockers cleared in `6383861`). No full criterion in 10–24 is closed. | New-version handling; born-digital/scanned production parsing; measured fallback; production-promoted canonical provenance; production-ready parent-child/table; production embedding promotion; human interrupt/resume; citations; sealed holdout; retrieval benchmarks; full lineage/reprocessing; complete offline ingestion. |
| 25–40 | Retrieval | Not proven | Phase 3 development slice adds active-only dense/lexical/exact hybrid candidate generation, intent-weighted RRF with inspectable component ranks, coverage matrix + honest abstention, and exact replay records against Phase 2D publications (partial evidence toward 28–31, 34–36, 38–39). No production cross-encoder/LLM reranker, expansion, multi-hop, injection suite, or promotion gates. Criteria remain open. | All required planes, identity/as-of, intent, expansion, fusion/rerank, coverage/contradiction/repair/abstention, injection containment, degradation, replay, and promotion gates. |
| 41–60 | Research, agents, and reports | Not proven | Phase 3 development slice adds Document Evidence specialist (extractive, no LLM), plan validation, assertion-before-prose, citation binding, versioned report draft gate, immutable review events, migration `0009`, and loopback research APIs (partial evidence toward 41–42, 47–48, 50–52, 58). Phase 5 adds a targeted research launch stub from monitoring alerts (plan-validated; no LangGraph)—partial toward 45 only. Remaining specialists, LangGraph interrupt/resume, calibrated judges, native Report Studio, multi-ecosystem due diligence, and criteria 4–8 remain open. | Typed validated plans/specialists/tools, three ecosystems, comparison, monitoring trigger, assertions-first, exact multi-plane citations, numerical validation, calibrated judges, immutable review, edit validation, complete lineage. |
| 61–84 | Structured data and monitoring | Not proven | Phase 4 development slice adds shared offline/fixture collector contracts, immutable raw envelopes, Bitcoin/EVM/Solana pinned observations, market decimal precision + sequence gap resnapshot, governance on/off-chain link, GitHub cursor/rate-limit metadata, bitemporal as-of reads, quality quarantine, leakage-safe feature eligibility, non-trading signals, migration `0010`, and loopback import/list APIs (partial evidence toward 61–78, 80–81). Phase 5 adds deterministic detection before triage, materiality screen, alert dedup/lifecycle, research invalidation, comparison/timeline contracts with envelope navigation, migration `0011`, and loopback monitoring APIs (partial evidence toward 82–84). Live collectors, DuckDB/Parquet writers, calibrated semantic triage, and native timeline UI remain blocked/out. Criteria remain open. | Shared raw envelope, scheduler controls, three chain families, market reconciliation/precision, governance/GitHub, bitemporal quality/replay, Parquet/DuckDB, leakage-safe features, non-trading signals, deterministic monitoring, alert/evidence navigation. |
| 85–102 | Models, evaluation, and observability | Not proven | Phase 1 through `647c25b` has isolated metadata-only OpenTelemetry, exact safe span/context registries, owner-private cross-process JSONL, raw-SDK defense, strict immutable model metadata and production-promotion refusal, an unavailable no-I/O provider, deterministic resource leases, real native diagnostics, fault recovery, and current development-component zero-egress evidence. Phase 6 development slice adds evaluation contracts (dataset splits, evaluator order, promotion outcomes), a frozen offline harness fixture (`fixtures/evaluation/retrieval_regression_v1.json`), deterministic evaluators preceding fail-closed `blocked_judge_uncalibrated`, and loopback `POST /v1/evaluation:run`—partial evidence toward 94–97 only. No real provider/model execution or qualification, calibrated judges, statistical CIs, distributed Swift-to-publication trace, retention system, or release-artifact proof. Criteria remain open. | Interchangeable qualified providers, Apple fallback, isolated services, load/unload/arbiter/recovery, immutable artifacts, six dataset splits, evaluator ordering/calibration/statistics, regression mining, distributed local traces/privacy/retention and disabled exporters. |
| 103–111 | Codex | Not proven | Phase 6 development slice adds product-plane contracts for sanitized reproduction bundles, approval policy, candidate patches, and always-denied authority (merge/push/deploy/promote/trade/sign); sanitize+gate implementation with secret redaction and no auto-apply; loopback `POST /v1/engineering/codex:gate`. This is not a qualified live Codex App Server / local provider suite. Criteria remain open. | Qualified local provider/app-server contracts, inspectable streams, isolated sanitized worktree, credential/private-data/network denial, approvals, no automatic authority, complete patch gate. |
| 112–134 | Security, packaging, and recovery | Not proven | Phase 1 has owner-private roots/sockets, Unix-only PostgreSQL, exact runtime capabilities, no root daemon or trading/wallet/signing code, bounded startup diagnostics, and recorded development `atlas doctor` zero-egress. PostgreSQL, artifact, and engine faults recover honestly. Phase 6 adds development filesystem backup/restore verify, Safe Mode capability mask, integrity scrub, CycloneDX-ish SBOM from `uv.lock` (`script/generate_sbom.py`), unsigned package inventory honesty, and fail-closed `script/release_check.py --require-release` (exits non-zero; reports `unsigned` / `notarization_blocked`). The staged app remains unsigned development evidence and still uses loopback HTTP. No Developer ID signing, notarization, stapling, Gatekeeper clean-user, Keychain-wrapped backup keys, or embedded signed Python. Criteria remain open. | Signed/notarized exact artifact, capability/entitlement matrix, no TCP release API, exact release zero egress/allowlists, Keychain/worker containment, supply-chain/SBOM, embedded runtime, install/update/rollback/backup/restore/Safe Mode/rebuild/scrub/doctor/uninstall/incident proof. |

## Appendix D

| Capability | Current status | Required completion artifact |
| --- | --- | --- |
| Immutable evidence | Not proven | Phase 2A adds raw selected-PDF publication, strict digest/size binding, append-only acquisition/decision/outbox evidence, exact duplicates, and restart persistence. Raw envelopes beyond PDF acquisition, supersession/new-version history, and full scrub remain. |
| Durable workflow | Not proven | Phase 2A proves hard engine termination during upload, safe orphan cleanup on restart, same-ID retry, and one durable acquisition/decision/outbox record. Phase 2B adds append-only parser-attempt started/terminal events and reconcile-abandoned recovery for preflight/parse. Publication resume and full workflow checkpoints do not exist. |
| PDF intelligence | Not proven | Phase 2B proves development-qualified Tier-0 born-digital parse (`pypdf`) and inspectable canonical pages with NFC/coordinate lineage; Docling stays blocked; OCR/scanned fallback, production promotion, and parser comparison gates remain. |
| Chunking | Not proven | Phase 2C implements five families with intrinsic goldens and non-searchable persistence/inspect APIs. Parent-child/table production-ready policy and labelled retrieval benchmark remain. |
| Retrieval | Not proven | Phase 2D proves development staging dense/lexical indexes and atomic publication activation under fixture embeddings. Phase 3 adds development hybrid RRF fusion, coverage/abstention, and replay records; production planes/rerank remain. |
| Multi-agent research | Not proven | Phase 3 adds Document Evidence specialist with schema-validated I/O and plan validation; remaining roster, LangGraph interrupt/resume, and repair loops remain. |
| Numerical integrity | Not proven | Calculation manifests, report revalidation, units/as-of tests. |
| Citation integrity | Not proven | Phase 3 binds direct_support citations with excerpt hashes before draft rendering; entailment judges and multi-plane locators remain. |
| Multi-chain | Not proven | Phase 4 adds offline Bitcoin/EVM/Solana fixture snapshots with pinned identity and reorg orphan stub; live nodes and full reorg/finality suites remain. |
| Market data | Not proven | Phase 4 fixture tick uses exact decimals and sequence-gap resnapshot; live snapshot/delta streams remain. |
| Monitoring | Not proven | Phase 5 development slice: deterministic change/materiality, deduplicated alert lifecycle, invalidation, targeted research launch stub, comparison/timeline payloads with envelope links. Calibrated triage and native UI remain. |
| Local models | Not proven | Capability registry, hashes/licenses, resource/crash/fallback qualification. |
| Evaluation | Not proven | Phase 6 offline harness + frozen fixture + deterministic evaluators + fail-closed uncalibrated judges; calibration/statistics/promotion remain. |
| Observability | Not proven | Phase 1 proves isolated metadata-only local spans, owner-private JSONL, privacy validation, and persistence; Swift-to-publication trace and retention remain. |
| Native application | Not proven | Phase 2A adds native PDF selection, bounded uploading, truthful quarantine/duplicate/rejection/failure presentation, same-request retry, stable accessibility IDs, compact/appearance/VoiceOver/keyboard/multi-window evidence. Phase 2B adds Evidence inspector controls for processing status and canonical page text/hashes. Parsed research surfaces and release proof remain. |
| Zero egress | Not proven | Development runtime and admission tests deny DNS, external TCP, mDNS, subprocess, parser, and model boundaries while allowing loopback plus the exact PostgreSQL socket. Phase 2B Seatbelt document-worker profile adds OS-enforced containment for parse/preflight children. Complete ingestion and exact signed release-artifact proof remain. |
| Codex | Not proven | Phase 6 product-plane sanitize/approval/gate/authority denial + loopback API; live App Server qualification remains. |
| Security | Not proven | Signed peers, sandbox/entitlements, redaction, malicious containment. |
| Release | Not proven | Phase 6 SBOM from lockfile + fail-closed unsigned release check; notarization/Gatekeeper/signed manifest remain blocked on secrets. |
| Recovery | Not proven | Phase 6 development filesystem backup/restore verify, Safe Mode, integrity scrub; Keychain-wrapped encrypted backup and fresh-install restore remain. |

## Update rule

Every reviewed vertical slice updates the relevant row with exact commands, fixture names, commit, and runtime artifact. The final audit expands each grouped Section 33 range into individual criterion evidence and rejects any row supported only by source presence or a narrower smoke.
