[build-system]
requires = ["hatchling==1.31.0"]
build-backend = "hatchling.build"

[project]
name = "rsi-atlas-retrieval"
version = "0.1.0"
description = "Hybrid retrieval, fusion, and evidence packet assembly"
license = "Apache-2.0"
requires-python = ">=3.11,<3.15"
dependencies = [
  "rsi-atlas-contracts==0.1.0",
  "rsi-atlas-storage==0.1.0",
]
