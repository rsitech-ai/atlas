[build-system]
requires = ["hatchling==1.31.0"]
build-backend = "hatchling.build"

[project]
name = "rsi-atlas-observability"
version = "0.1.0"
description = "Privacy-safe local tracing for RSI Atlas"
license = "Apache-2.0"
requires-python = ">=3.11,<3.15"
dependencies = [
  "opentelemetry-api==1.44.0",
  "opentelemetry-sdk==1.44.0",
]
