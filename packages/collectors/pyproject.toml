[build-system]
requires = ["hatchling==1.31.0"]
build-backend = "hatchling.build"

[project]
name = "rsi-atlas-collectors"
version = "0.1.0"
description = "Offline fixture collectors, observation pipeline, and fail-closed live stubs"
requires-python = ">=3.11,<3.15"
dependencies = [
  "rsi-atlas-contracts==0.1.0",
]
