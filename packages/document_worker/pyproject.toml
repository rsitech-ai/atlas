[build-system]
requires = ["hatchling==1.31.0"]
build-backend = "hatchling.build"

[project]
name = "rsi-atlas-document-worker"
version = "0.1.0"
description = "Isolated document-worker process for RSI Atlas PDF preflight and parsing"
license = "Apache-2.0"
requires-python = ">=3.11,<3.15"
dependencies = [
  "rsi-atlas-contracts==0.1.0",
  "pypdf==6.14.2",
  "pdfminer-six==20260107",
]
