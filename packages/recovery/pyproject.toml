[build-system]
requires = ["hatchling==1.31.0"]
build-backend = "hatchling.build"

[project]
name = "rsi-atlas-recovery"
version = "0.1.0"
description = "Development backup, restore, Safe Mode, and integrity scrub"
license = "Apache-2.0"
requires-python = ">=3.11,<3.15"
dependencies = [
  "cryptography>=44.0.0,<50",
  "rsi-atlas-contracts==0.1.0",
]
