[build-system]
requires = ["hatchling==1.31.0"]
build-backend = "hatchling.build"

[project]
name = "rsi-atlas-contracts"
version = "0.1.0"
description = "Versioned contracts for RSI Atlas process boundaries"
requires-python = ">=3.11,<3.15"
dependencies = [
  "pydantic==2.13.4",
]
